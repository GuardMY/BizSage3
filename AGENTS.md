## 功能变更
涉及功能新增或变更的请求，在给出最终方案或实施前，必须先向用户提问，且一次只问一个问题。根据用户回答继续追问，直到约有 95% 的信心完全理解用户的真实需求、目标、边界和验收标准。
如果有非用户明确的需求，但有利于核心功能实现，需要明确得到用户的同意再规划实现

对于不精确的信息，能使用LLM进行总结的，尽可能用LLM进行

## codex相关
codex对话栏涉及代码引用时，给出可点击跳转的形式
codex对话时，如需用户给出是否，或者有限性选择时，给出快捷选择框

## 部署测试
代码部署在192.168.31.91 的docker环境。目录为~/project/BizSage3，SSH用户为root，默认端口，认证在当前环境SSH默认key。
一轮功能变更后，不要在本地起服务。因测试起的本地服务，测试后关闭
需要问题排查时，利用远程环境进行排查