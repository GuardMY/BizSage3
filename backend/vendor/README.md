# Vendored AgentTrace

`agenttrace-0.1.0-py3-none-any.whl` is copied from the AgentTrace `0.1.0`
build output and installed explicitly by `backend/Dockerfile`.

SHA-256:

```text
2F40751B02BBA15BF1F7D07CCD35EEC59AF6444528E547215F247529468EC809
```

When updating AgentTrace, replace the wheel, update its filename in the
Dockerfile and local setup documentation, and refresh this checksum.
